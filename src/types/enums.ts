export enum Environment {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production'
}

export enum Path {
  VALIDATOR = '/validator'
}

export enum Severity {
  VIOLATION = 'Violation',
  WARNING = 'Warning',
  INFO = 'Info'
}
